name: board_game_app
description: "Board game app"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  drift_flutter: ^0.2.8
  drift: ^2.30.0
  intl: ^0.20.2
  flutter_bloc: ^9.1.1
  get_it: ^9.2.0
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.28.1
  build_runner: ^2.7.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
